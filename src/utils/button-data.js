/**
 * Pass this button configuration on button component to render and alter the look and feel
 * color values can be: 'primary', 'secondary', 'success', 'info', 'warning', 'danger', 'link'
 */

export const config = {
  active: true,
  block: false,
  color: 'primary', // default: 'secondary'
  disabled: false,
  size: 'md',
  outline: false
};
